{% extends 'base.html.twig' %}

{% block title %}New reward request{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <h1>Request reward</h1>
            
            <div class="alert alert-info mb-4">
                <strong>Reward:</strong> {{ recompense.recompense }}
                <br>
                <strong>Type:</strong> {{ recompense.type }}
                <br>
                <strong>Rank:</strong> <span class="badge bg-info">{{ recompense.classement }}</span>
            </div>

            {% if app.flashes %}
                {% for type, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="alert alert-{{ type }} alert-dismissible fade show" role="alert">
                            {{ message }}
                            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                        </div>
                    {% endfor %}
                {% endfor %}
            {% endif %}

            <div class="card">
                <div class="card-body">
                    {{ form_start(form, {'attr': {'class': 'form', 'novalidate': true}}) }}
                    
                    <div class="mb-3">
                        {{ form_label(form.nomDemandeur) }}
                        {{ form_widget(form.nomDemandeur) }}
                        {% if form.nomDemandeur.vars.errors|length > 0 %}
                            <div class="text-danger">
                                {{ form_errors(form.nomDemandeur) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.email) }}
                        {{ form_widget(form.email) }}
                        {% if form.email.vars.errors|length > 0 %}
                            <div class="text-danger">
                                {{ form_errors(form.email) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.motif) }}
                        {{ form_widget(form.motif) }}
                        {% if form.motif.vars.errors|length > 0 %}
                            <div class="text-danger">
                                {{ form_errors(form.motif) }}
                            </div>
                        {% endif %}
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success">
                            <i class="fas fa-check"></i> Submit request
                        </button>
                        <a href="{{ path('recompense_show', {'id': recompense.id}) }}" class="btn btn-secondary">
                            <i class="fas fa-times"></i> Cancel
                        </a>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
