<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Report - Rewards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            color: #333;
        }
        h1 {
            text-align: center;
            color: #0066cc;
            border-bottom: 3px solid #0066cc;
            padding-bottom: 10px;
        }
        h2 {
            color: #0066cc;
            margin-top: 30px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th {
            background-color: #0066cc;
            color: white;
            padding: 12px;
            text-align: left;
            border: 1px solid #0066cc;
        }
        td {
            padding: 10px;
            border: 1px solid #ddd;
        }
        tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        tr:hover {
            background-color: #f0f0f0;
        }
        .statistics {
            background-color: #e6f2ff;
            padding: 15px;
            border-left: 4px solid #0066cc;
            margin: 20px 0;
        }
        .stats-item {
            display: inline-block;
            margin-right: 30px;
        }
        .stats-value {
            font-size: 24px;
            font-weight: bold;
            color: #0066cc;
        }
        .page-break {
            page-break-after: always;
        }
        footer {
            margin-top: 40px;
            text-align: center;
            color: #999;
            font-size: 12px;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }
    </style>
</head>
<body>
    <h1>Rewards Report</h1>
    
    <div class="statistics">
        <div class="stats-item">
            <span>Total rewards:</span>
            <div class="stats-value">{{ recompenses|length }}</div>
        </div>
        <div class="stats-item">
            <span>Minimum rank:</span>
            <div class="stats-value">{{ minClassement ?? 'N/A' }}</div>
        </div>
        <div class="stats-item">
            <span>Maximum rank:</span>
            <div class="stats-value">{{ maxClassement ?? 'N/A' }}</div>
        </div>
    </div>

    <h2>Reward details</h2>
    
    {% if recompenses|length > 0 %}
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Type</th>
                    <th>Rank</th>
                    <th>Description</th>
                    <th>Requests</th>
                </tr>
            </thead>
            <tbody>
                {% for recompense in recompenses %}
                    <tr>
                        <td>{{ recompense.id }}</td>
                        <td><strong>{{ recompense.recompense }}</strong></td>
                        <td>{{ recompense.type }}</td>
                        <td style="text-align: center;">{{ recompense.classement }}</td>
                        <td>{{ recompense.description ?? '-' }}</td>
                        <td style="text-align: center;">{{ recompense.demandes|length }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p style="color: #cc6600; text-align: center;">No rewards found.</p>
    {% endif %}

    <footer>
        <p>Report generated on {{ "now"|date('d/m/Y à H:i:s') }}</p>
        <p>Rewards Management - E-Sports System</p>
    </footer>
</body>
</html>
