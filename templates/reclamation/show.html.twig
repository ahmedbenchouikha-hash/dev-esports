{% extends "base.html.twig" %}

<<<<<<< HEAD
{% block title %}Reclamation Details - Dev Esports{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .module-show-shell {
            background: rgba(30, 41, 79, 0.72);
            border: 1px solid rgba(71, 85, 105, 0.55);
            border-radius: 14px;
            box-shadow: 0 12px 30px rgba(2, 6, 23, 0.35);
        }

        .module-show-subtitle {
            margin-top: 0.35rem;
            color: #94a3b8;
            font-size: 0.92rem;
        }
    </style>
{% endblock %}
=======
{% block title %}Reclamation #{{ reclamation.id }} - Dev Esports{% endblock %}
>>>>>>> module-rewards

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
<<<<<<< HEAD
        <div>
            <h1 class="mb-0">Reclamation Details</h1>
            <p class="module-show-subtitle">Consultez le statut, les informations et les suites administratives de la demande.</p>
        </div>
        <div>
            {% if not is_granted('ROLE_ADMIN') %}
                <form method="POST" action="{{ path('app_reclamation_delete', {'id': reclamation.id}) }}" style="display: inline;" onsubmit="return confirm('Delete this reclamation?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
                    <button type="submit" class="crud-icon-btn crud-icon-btn-delete" title="Delete" aria-label="Delete">
                        <i class="fas fa-times"></i>
                    </button>
                </form>
            {% endif %}
        </div>
    </div>

    <div class="card mb-4 module-show-shell">
=======
        <h1>Reclamation #{{ reclamation.id }}</h1>
        <div>
            <a href="{{ path('app_reclamation_edit', {'id': reclamation.id}) }}" class="btn btn-warning">
                <i class="fas fa-edit"></i> Edit
            </a>
            <form method="POST" action="{{ path('app_reclamation_delete', {'id': reclamation.id}) }}" style="display: inline;" onsubmit="return confirm('Delete this reclamation?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-trash"></i> Delete
                </button>
            </form>
        </div>
    </div>

    <div class="card mb-4">
>>>>>>> module-rewards
        <div class="card-header">
            <h5>{{ reclamation.titre }}</h5>
        </div>
        <div class="card-body">
            <div class="row mb-3">
                <div class="col-md-6">
                    <h6 class="text-muted">Status</h6>
<<<<<<< HEAD
                    {% if is_granted('ROLE_ADMIN') and reclamation.etat.value == 'EN_COURS' %}
                    <div class="dropdown">
                        <button class="btn btn-sm btn-warning dropdown-toggle" type="button" id="stateDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            <span id="stateBadge" class="badge bg-warning">{{ reclamation.etat.value|replace({'_': ' '}) }}</span>
                        </button>
                        <ul class="dropdown-menu reclamation-state-menu" aria-labelledby="stateDropdown">
                            <li>
                                <a class="reclamation-state-option reclamation-state-option-resolved" href="#" onclick="changeReclamationState({{ reclamation.id }}, 'RESOLU'); return false;">
                                    ✓ Résolu
                                </a>
                            </li>
                            <li>
                                <a class="reclamation-state-option reclamation-state-option-rejected" href="#" onclick="changeReclamationState({{ reclamation.id }}, 'REJETE'); return false;">
                                    ✗ Rejeté
                                </a>
                            </li>
                        </ul>
                    </div>
                    {% else %}
                    <p><span class="badge {% if reclamation.etat.value == 'EN_COURS' %}bg-warning{% elseif reclamation.etat.value == 'RESOLU' %}bg-success{% else %}bg-danger{% endif %}">{{ reclamation.etat.value|replace({'_': ' '}) }}</span></p>
                    {% endif %}
=======
                    <p><span class="badge {% if reclamation.etat.value == 'EN_COURS' %}bg-warning{% elseif reclamation.etat.value == 'RESOLU' %}bg-success{% else %}bg-danger{% endif %}">{{ reclamation.etat.value|replace({'_': ' '}) }}</span></p>
>>>>>>> module-rewards
                </div>
                <div class="col-md-6">
                    <h6 class="text-muted">Type</h6>
                    <p>{{ reclamation.type.value|replace({'_': ' '}) }}</p>
                </div>
            </div>

            <div class="row mb-3">
                <div class="col-md-12">
                    <h6 class="text-muted">Description</h6>
                    <p>{{ reclamation.description }}</p>
                </div>
            </div>

            {% if reclamation.player %}
            <div class="row mb-3">
                <div class="col-md-6">
                    <h6 class="text-muted">Player</h6>
                    <p>{{ reclamation.player.nickname }}</p>
                </div>
            </div>
            {% endif %}

            <div class="row">
                <div class="col-md-6">
                    <h6 class="text-muted">Created</h6>
                    <p>{{ reclamation.createdAt|date('d/m/Y H:i') }}</p>
                </div>
                <div class="col-md-6">
                    <h6 class="text-muted">Updated</h6>
                    <p>{{ reclamation.updatedAt|date('d/m/Y H:i') }}</p>
                </div>
            </div>
        </div>
    </div>

    {% if reclamation.adminResponse %}
    <div class="card mb-4 border-success">
        <div class="card-header bg-success text-white">
            <h5>Admin Response</h5>
        </div>
        <div class="card-body">
            <p>{{ reclamation.adminResponse.message }}</p>
            <small class="text-muted">Responded: {{ reclamation.adminResponse.createdAt|date('d/m/Y H:i') }}</small>
        </div>
    </div>
    {% endif %}

    {% if reclamation.punition %}
    <div class="card mb-4 border-danger">
        <div class="card-header bg-danger text-white">
            <h5>Punishment Applied</h5>
        </div>
        <div class="card-body">
<<<<<<< HEAD
            <p>
                <strong>Bans:</strong>
                <span class="ms-1 align-middle d-inline-block">
                    {% include 'punition/_ban_chips.html.twig' with { banList: reclamation.punition.banList, containerClass: 'd-inline-flex flex-wrap gap-1' } only %}
                </span>
            </p>
=======
            <p><strong>Player Status:</strong> <span class="badge bg-danger">{{ reclamation.punition.playerStatus }}</span></p>
>>>>>>> module-rewards
            <p><strong>From:</strong> {{ reclamation.punition.startAt|date('d/m/Y H:i') }}</p>
            <p><strong>To:</strong> {{ reclamation.punition.endAt|date('d/m/Y H:i') }}</p>
            <a href="{{ path('app_punition_show', {'id': reclamation.punition.id}) }}" class="btn btn-sm btn-danger">View Punishment</a>
        </div>
    </div>
    {% endif %}

    <a href="{{ path('app_reclamation_index') }}" class="btn btn-secondary">Back to List</a>
</div>
{% endblock %}
