{% extends 'base.html.twig' %}

<<<<<<< HEAD
{% block title %}{{ tournament.name }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="{{ path('tournament_index') }}">Tournaments</a></li>
            <li class="breadcrumb-item active">{{ tournament.name }}</li>
        </ol>
    </nav>

    {# Display flash messages #}
    {% for label, messages in app.flashes %}
        {% for message in messages %}
            <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
                {{ message }}
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        {% endfor %}
    {% endfor %}

    <div class="row">
        <div class="col-md-8">
            <h1>{{ tournament.name }}</h1>

            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="mb-0">Tournament Details</h5>
                </div>
                <div class="card-body">
                    <p>{{ tournament.description }}</p>

                    <table class="table table-hover">
                        <tr>
                            <th style="width: 150px;">Game</th>
                            <td>{{ tournament.game ?? 'Not specified' }}</td>
                        </tr>
                        <tr>
                            <th>Start Date</th>
                            <td>{{ tournament.startDate|date('F j, Y H:i') }}</td>
                        </tr>
                        <tr>
                            <th>End Date</th>
                            <td>{{ tournament.endDate|date('F j, Y H:i') }}</td>
                        </tr>
                        <tr>
                            <th>Location</th>
                            <td>{{ tournament.location }}</td>
                        </tr>
                        <tr>
                            <th>Prize Pool</th>
                            <td><span class="text-success fw-bold">${{ tournament.prizePool|number_format(0, '.', ',') }}</span></td>
                        </tr>
                        <tr>
                            <th>Status</th>
                            <td>
                                {% set status_color = {
                                    'pending': 'warning',
                                    'published': 'success',
                                    'ongoing': 'primary',
                                    'completed': 'secondary',
                                    'cancelled': 'danger'
                                } %}
                                <span class="badge bg-{{ status_color[tournament.status] ?? 'secondary' }}">
                                    {{ tournament.status|capitalize }}
                                </span>
                            </td>
                        </tr>
                    </table>
                </div>
            </div>

            {% if tournament.rules %}
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Tournament Rules</h5>
                    </div>
                    <div class="card-body">
                        <ol class="mb-0">
                            {% for rule in tournament.rules %}
                                <li class="mb-2">{{ rule }}</li>
                            {% endfor %}
                        </ol>
                    </div>
                </div>
            {% endif %}
        </div>

        <div class="col-md-4">
            <div class="card tournament-actions-sticky">
                <div class="card-header">
                    <h5 class="mb-0">Actions</h5>
                </div>
                <div class="card-body">
                    {% if tournament.status == 'pending' %}
                        {% if is_granted('ROLE_USER') %}
                            <a href="{{ path('tournament_join_form', {'id': tournament.id}) }}" class="btn btn-success btn-lg w-100 mb-2">
                                <i class="fas fa-plus-circle"></i> Join Tournament
                            </a>
                        {% else %}
                            <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg w-100 mb-2">
                                <i class="fas fa-sign-in-alt"></i> Login to Join
                            </a>
                        {% endif %}
                    {% endif %}

                    <a href="{{ path('tournament_index') }}" class="btn btn-secondary w-100">
                        <i class="fas fa-arrow-left"></i> Back to List
                    </a>
                </div>
            </div>
=======
{% block title %}{{ tournament.name }} - Dev Esports{% endblock %}

{% block body %}
<div class="row mb-4">
    <div class="col-md-12">
        <a href="{{ path('tournament_index') }}" class="btn btn-outline-primary btn-sm">← Back to Tournaments</a>
    </div>
</div>

<div class="row">
    <div class="col-lg-8 mx-auto">
        <div class="card mb-4">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-start mb-4">
                    <h1 class="page-title">{{ tournament.name }}</h1>
                    <span class="badge {% if tournament.status == 'completed' %}badge-success{% elseif tournament.status == 'ongoing' %}badge-danger{% elseif tournament.status == 'pending' %}badge-warning{% else %}badge-secondary{% endif %}" style="font-size: 1rem;">
                        {{ tournament.status|capitalize }}
                    </span>
                </div>
                
                {% if tournament.description %}
                    <div class="mb-3">
                        <strong>Description:</strong>
                        <p>{{ tournament.description }}</p>
                    </div>
                {% endif %}
                
                <hr>
                
                <!-- Tournament Details -->
                <div class="row mb-4">
                    <div class="col-md-6">
                        <p><strong>📅 Start Date:</strong> {{ tournament.startDate|date('d M Y') }}</p>
                        <p><strong>📅 End Date:</strong> {{ tournament.endDate|date('d M Y') }}</p>
                    </div>
                    <div class="col-md-6">
                        <p><strong>📍 Location:</strong> {{ tournament.location ?? 'TBD' }}</p>
                        <p><strong>💰 Prize Pool:</strong> {% if tournament.prizePool %}${{ tournament.prizePool|number_format(2) }}{% else %}TBD{% endif %}</p>
                    </div>
                </div>

                <!-- Matches Section -->
                {% if tournament.games|length > 0 %}
                    <hr>
                    <h3>Matches</h3>
                    <div class="row">
                        {% for match in tournament.games %}
                            <div class="col-md-6 mb-3">
                                <div class="card">
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                            <h6 class="card-title mb-0">{{ match.team1.name }} vs {{ match.team2.name }}</h6>
                                            <span class="badge {% if match.status == 'finished' %}badge-success{% elseif match.status == 'ongoing' %}badge-danger{% else %}badge-warning{% endif %}" style="font-size: 0.75rem;">
                                                {{ match.status|capitalize }}
                                            </span>
                                        </div>
                                        <p class="mb-1">
                                            <strong style="color: var(--highlight-color);">{{ match.score1 }} - {{ match.score2 }}</strong>
                                        </p>
                                        <small class="text-muted">{{ match.matchdate|date('d M Y - H:i') }}</small><br>
                                        <a href="{{ path('match_show', { id: match.id }) }}" class="btn btn-sm btn-outline-primary mt-2">View Match</a>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% else %}
                    <div class="alert alert-info">
                        No matches scheduled yet for this tournament.
                    </div>
                {% endif %}
            </div>
        </div>

        <div class="text-center">
            <a href="{{ path('tournament_index') }}" class="btn btn-outline-primary">← Back to Tournaments</a>
>>>>>>> module-rewards
        </div>
    </div>
</div>
{% endblock %}
